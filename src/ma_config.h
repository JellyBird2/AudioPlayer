// Shared miniaudio configuration. Include BEFORE miniaudio.h in every TU.
// Device playback + WAV encoder on; all bundled decoders on (WAV/FLAC/MP3/
// Vorbis). Everything else off to stay small.
#pragma once
